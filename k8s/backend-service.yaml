apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: afribiz
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      name: http
    - port: 9464
      targetPort: 9464
      name: metrics
  selector:
    app: backend
