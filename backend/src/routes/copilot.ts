import { Router } from 'express';

const router = Router();

// Copilot routes are served through dataHubAnalytics routes
// This file reserved for future rule-based Copilot endpoints

export default router;
